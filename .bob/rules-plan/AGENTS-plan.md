# Spice Maker: Plan mode

- Read the root `AGENTS.md` and `.bob/rules/project-safety.md` before planning changes.
- State the files to change, verification to run, and any external prerequisite such as a user-selected LTspice path or a provider key.
- Keep all generated data inside this extracted copy. Preserve the rule that quick drafts cannot receive an electrical PASS.
- Treat IBM Bob's project files as workspace guidance; the application's model authoring and verification remain application-owned.
